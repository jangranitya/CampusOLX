<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Profile</title>
  <style>
    body {
      background-color: #f3e5f5;
      color: rgb(59, 10, 69);
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      background-image: url('/campus2.jpg'); /* Update with correct path */
      background-size: cover;
      background-attachment: fixed;
    }

    .container {
      background-color: white;
      padding: 30px;
      border-radius: 10px;
      max-width: 400px;
      width: 100%;
      box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
    }

    h1 {
      color: rgb(59, 10, 69);
      text-align: center;
      font-size: 2em;
      margin-bottom: 20px;
    }

    .form-group {
      display: flex;
      flex-direction: column;
      margin-bottom: 15px;
    }

    label {
      font-weight: bold;
      color: rgb(59, 10, 69);
    }

    input[type="text"],
    input[type="file"] {
      padding: 10px;
      font-size: 1em;
      border: 1px solid #d1c4e9;
      border-radius: 5px;
      margin-top: 5px;
    }

    input[type="file"] {
      padding: 5px;
    }

    p {
      color: rgb(59, 10, 69);
      font-style: italic;
      margin: 0;
      padding: 8px 0;
    }

    .submit-btn {
      width: 100%;
      padding: 10px;
      margin-top: 20px;
      background-color: rgb(59, 10, 69);
      color: white;
      text-align: center;
      border: none;
      border-radius: 8px;
      font-size: 1.1em;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    .submit-btn:hover {
      background-color: #ab47bc;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Edit Profile</h1>
    
    <form action="/update-profile" method="POST" enctype="multipart/form-data">
      <div class="form-group">
        <label>Email:</label>
        <p><%= userProfile.email %></p>
      </div>
      
      <div class="form-group">
        <label>Name:</label>
        <input type="text" name="name" value="<%= userProfile.name || '' %>" />
      </div>
      
      <div class="form-group">
        <label>Enrollment Number:</label>
        <input type="text" name="enroll_no" value="<%= userProfile.enroll_no || '' %>" />
      </div>
      
      <div class="form-group">
        <label>Contact Number:</label>
        <input type="text" name="contact" value="<%= userProfile.contact || '' %>" />
      </div>
      
      <div class="form-group">
        <label>Room Number:</label>
        <input type="text" name="room_no" value="<%= userProfile.room_no || '' %>" />
      </div>
      
      <div class="form-group">
        <label>Profile Photo:</label>
        <input type="file" name="profilePhoto" />
      </div>

      <button type="submit" class="submit-btn">Save Changes</button>
    </form>
  </div>
</body>
</html>
