<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Post</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color:white;
            color: rgb(59, 10, 69);
            font-family: Arial, sans-serif;
            background-image: url("/campus2.jpg"); /* Add your image URL here */
            background-size: cover;
            background-attachment: fixed;
        }

        h2 {
            color: rgb(59, 10, 69);
            text-align: center;
            margin-top: 30px;
            font-size: 2rem;
            font-weight: bold;
        }

        main {
            max-width: 600px;
            margin: 30px auto;
            padding: 20px;
            background-color: rgba(255, 255, 255, 0.9); /* Slight transparency for the background */
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        label {
            font-size: 1.1rem;
            margin-bottom: 5px;
            display: block;
            font-weight: bold;
        }

        input[type="text"],
        input[type="email"],
        input[type="tel"],
        input[type="number"],
        textarea,
        input[type="file"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 2px solid rgb(59, 10, 69);
            border-radius: 5px;
            font-size: 1rem;
        }

        textarea {
            height: 150px;
            resize: vertical;
        }

        button {
            background-color: rgb(59, 10, 69);
            color: white;
            border: none;
            padding: 10px 15px;
            font-size: 1.1rem;
            border-radius: 5px;
            cursor: pointer;
            width: 100%;
            margin-top: 10px;
        }

        button:hover {
            background-color: darkmagenta;
        }

        .current-image {
            margin-top: 20px;
            text-align: center;
        }

        .current-image img {
            width: 200px;
            height: auto;
            border-radius: 8px;
        }

        footer {
            background-color: rgb(59, 10, 69);
            color: white;
            text-align: center;
            padding: 15px;
            position: fixed;
            width: 100%;
            bottom: 0;
        }
    </style>
</head>
<body>
    <main>
        <h2>Edit Product</h2>
        <form action="/edit/<%= post.id %>" method="POST" enctype="multipart/form-data">
            <!-- Product Title -->
            <label for="title">Product Name:</label>
            <input type="text" id="title" name="title" value="<%= post.title %>" required>

            <!-- Product Description -->
            <label for="content">Description:</label>
            <textarea id="content" name="content" required><%= post.content %></textarea>

            <!-- Mobile Number -->
            <label for="mobile">Mobile Number:</label>
            <input type="tel" id="mobile" name="mobile" value="<%= post.mobile_number %>" required pattern="[0-9]{10}" placeholder="Enter 10-digit mobile number">

            <!-- Email -->
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" value="<%= post.email_address %>" required placeholder="Enter your email address">

            <!-- Expected Amount -->
            <label for="amount">Expected Amount:</label>
            <input type="number" id="amount" name="amount" value="<%= post.expected_amount %>" required min="0" placeholder="Enter expected amount">

            <!-- Display current image if exists -->
            <% if (post.imageUrl) { %>
                <div class="current-image">
                    <p>Current Image:</p>
                    <img src="<%= post.imageUrl %>" alt="<%= post.title %>">
                </div>
            <% } %>

            <!-- Upload new image -->
            <label for="image">Replace Image (optional):</label>
            <input type="file" id="image" name="image">

            <!-- Submit Button -->
            <button type="submit">Save Changes</button>
        </form>
    </main>

    <footer>
        <p>&copy; Campus Bazaar</p>
    </footer>
</body>
</html>